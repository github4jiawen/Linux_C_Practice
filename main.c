/*main.c*/
#include "stdlib.h"
#include "binarytree.h"

void print_item(link p)
{
	printf("%d\n", p->item);
}

int main(int argc, char const *argv[])
{
	unsigned char pre_seq[]=
	unsigned char in_seq[]=
	link root=tree_init(pre_seq,in_seq,7);
	pre_order
	return 0;
}